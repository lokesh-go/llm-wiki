# Vocab Index
# AI: Read this entire file before /compile and /query. It is tiny (~200 tokens).
# To get term definitions: read vocabs/<category>.md
# Format: category: term1, term2, ...
# To add a new term: append to the correct line AND to vocabs/<category>.md

system-design:   scalability, availability, consistency, partition-tolerance,
                 load-balancing, caching, sharding, replication, CAP-theorem,
                 fault-tolerance, rate-limiting, back-pressure, idempotency,
                 circuit-breaker, service-discovery, API-gateway

architecture:    microservices, monolith, serverless, edge-computing,
                 event-driven-architecture, hexagonal-architecture,
                 clean-architecture, DDD, CQRS, event-sourcing, saga-pattern,
                 strangler-fig, BFF-pattern

design-patterns: singleton, factory-method, abstract-factory, builder, prototype,
                 adapter, bridge, composite, decorator, facade, flyweight, proxy,
                 chain-of-responsibility, command, iterator, mediator, memento,
                 observer, state, strategy, template-method, visitor, pub-sub

practices:       TDD, BDD, CI-CD, trunk-based-development, feature-flags,
                 observability, SLO-SLA-SLI, chaos-engineering,
                 blue-green-deployment, canary-release, IaC

data-storage:    SQL, NoSQL, graph-database, time-series-database,
                 eventual-consistency, ACID, BASE-theorem, normalization,
                 indexing, query-optimization, connection-pooling

technologies:    Kafka, RabbitMQ, Redis, Memcached, PostgreSQL, MySQL,
                 MongoDB, Elasticsearch, Kubernetes, Docker, gRPC, GraphQL,
                 REST, WebSocket, React, Next.js, Go, Rust, Python,
                 TypeScript, Nginx, HAProxy, AWS, GCP, Azure, Vercel, Cloudflare

languages:       goroutine, channel, select-statement, interface-go, WaitGroup,
                 context-go, error-handling-go, defer,
                 event-loop-node, async-await, promise, streams-node,
                 middleware-node, cluster-module,
                 generics-ts, utility-types-ts, type-guard-ts, decorators-ts,
                 mapped-types-ts, conditional-types-ts, discriminated-union-ts,
                 declaration-merging-ts,
                 ownership-rust, borrowing-rust, lifetimes-rust, traits-rust,
                 smart-pointers-rust, async-rust, error-handling-rust, pattern-matching-rust
